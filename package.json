{
  "name": "hello-react-flask",
  "version": "1.0.0",
  "description": "A simple React single page app, built with Flask, Vite and React.",
  "scripts": {
    "start": "vite",
    "server": "source ./.venv/bin/activate && PYTHONUNBUFFERED=true python3 server.py",
    "glitch-start": "pip3 install -r requirements.txt && vite build && PYTHONUNBUFFERED=true python3 server.py",
    "build": "source ./.venv/bin/activate && pip3 install -r requirements.txt && vite build",
    "vite-serve": "vite preview",
    "serve": "source ./.venv/bin/activate && PYTHONUNBUFFERED=true python3 server.py"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-helmet-async": "^1.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^3.1.0",
    "vite": "^4.1.5"
  },
  "browserslist": [
    ">0.2%",
    "not dead"
  ],
  "engines": {
    "node": "14.x"
  }
}
